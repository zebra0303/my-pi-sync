# Global Agent Instructions

Keep this file short. Store detailed conventions under `~/.pi/agent/docs/conventions/` and link them here.

## Required global conventions

- Workflow and communication: `~/.pi/agent/docs/conventions/workflow.md`
- Code quality: `~/.pi/agent/docs/conventions/code-quality.md`
- Testing: `~/.pi/agent/docs/conventions/testing.md`
- Frontend: `~/.pi/agent/docs/conventions/frontend.md`
- Personal context: `~/.pi/agent/docs/conventions/personal-context.md`
- Git convention: `~/.pi/agent/docs/conventions/git.md`

## Operating rules

- Follow the required global conventions above for all pi work.
- Project-level `AGENTS.md` files may add stricter or more specific rules.
- If global and project instructions conflict, follow the more specific project instruction unless it violates safety or explicit user direction.
- For commits, pull requests, or code review guidance, read and follow the git convention linked above.
