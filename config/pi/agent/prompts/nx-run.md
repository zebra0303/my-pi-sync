---
description: Run or debug Nx targets through the workspace package manager
argument-hint: "<target-or-command> [projects]"
---

Use the `nx-run-tasks` skill. If project or target discovery is needed, use `nx-workspace` first.

Arguments: $ARGUMENTS
