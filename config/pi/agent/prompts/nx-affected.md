---
description: Run Nx affected checks and explain failures
argument-hint: "[base-branch]"
---

Use the `nx-run-tasks` skill.
Run affected checks for the current branch against the provided base branch, defaulting to `origin/main`.

Arguments: $ARGUMENTS

Default target set: `lint test build`.
Summarize commands executed, result, failure cause candidates, and focused follow-up validation commands.
