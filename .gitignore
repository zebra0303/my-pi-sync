# macOS
.DS_Store

# pi secrets / volatile data: never commit these
auth.json
sessions/
bin/
git/
npm/
*.jsonl

# omp secrets / volatile data: never commit these
.env
.env.*
secrets.yml
secret-placeholder.key
*.token
install-id
*.db
*.db-shm
*.db-wal

# Node
node_modules/

# local logs / scratch
*.log
.tmp/
